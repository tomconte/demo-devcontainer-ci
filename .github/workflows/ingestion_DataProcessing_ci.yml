# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

name: Ingestion DataProcessing CI

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
    - 'src/ingestion/DataProcessing/src/**'
    - '.devcontainer/**'
  pull_request:
    branches: [ "main" ]
    paths:
    - 'src/ingestion/DataProcessing/src/**'
    - '.devcontainer/**'

jobs: 
  build:
    uses: ./.github/workflows/ingestion_DataProcessing_ci_template.yml
